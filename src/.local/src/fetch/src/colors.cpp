#include "colors.hpp"

#include <string>

#include "config.hpp"

// Now uses config.hpp constants
const std::string kColCharSpace(kColSpace, ' ');

const std::string kColorsInfo =
    kCol3 + kColChar1 + kColCharSpace + kCol2 + kColChar2 + kColCharSpace +
    kCol1 + kColChar3 + kColCharSpace + kCol4 + kColChar4 + kColCharSpace +
    kCol5 + kColChar5 + kColCharSpace + kCol6 + kColChar6 + kColCharSpace +
    kCol7 + kColChar7 + kColCharSpace + kCol8 + kColChar8 + kColCharSpace +
    kColR;
